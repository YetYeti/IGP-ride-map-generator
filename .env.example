# 应用配置
NODE_ENV=development
PORT=3000

# 存储配置（仅在 NODE_ENV=production 时使用 /var/lib/igpsport/*）
# 本地开发环境默认使用 public/tasks、public/output 等目录
OUTPUT_DIR=/var/lib/igpsport/output
TASKS_DIR=/var/lib/igpsport/tasks
FIT_DIR=/var/lib/igpsport/fit

# 日志配置
LOG_DIR=/var/log/igpsport
LOG_LEVEL=info

# Redis 配置（可选）
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=
